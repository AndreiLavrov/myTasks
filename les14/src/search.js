/*
export class Search {
	constructor (){
		this.search = document.querySelector('.search-line .search');
		this.init();
		this.filterString = '';
	}

	init(){
		this.search.addEventListener('input', this.funTreatment);
	}

	funTreatment(event) {
			if(event.target.value.trim() === '') {                         // проверить на чило и т.д.
				this.filterString = '';
			}

		this.filterString = event;
	}
}
*/
