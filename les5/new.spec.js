describe('new.js -> clock -> return the number of seconds until tomorrow', function () {

	/* beforeEach(function () {

				spyOn(window, 'Date').and.callFake(function() {
						return new Date(1991,06,18);
				});

		});
		it('should return 0', function () {
				const res = clock();
				console.log(res);
				expect(res).toEqual(0);
		} );

*/
} );
